import { PureExpontialPipe } from './pure-expontial.pipe';

describe('PureExpontialPipe', () => {
  it('create an instance', () => {
    const pipe = new PureExpontialPipe();
    expect(pipe).toBeTruthy();
  });
});
